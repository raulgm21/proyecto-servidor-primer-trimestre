window.onload = function (){


    // --- Cuando buscas por el navegador y pulsas enter --- //

    var navegador = document.getElementById('navegador');       // Captura la ID del input

    navegador.addEventListener('keyup', function(e) {           // Evento de presionar tecla
            var tecla = e.keyCode || e.which;
            if (tecla == 13) {                                  // El número 13 pertenece al ENTER
              
            }
        }
    );
    

}






   

